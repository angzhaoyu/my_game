该文件用于解释文件夹结构。
该项目由前端和后端组成，前端基于cocos。后端数据储存基于mysql。

后端文件夹为backend。

前端文件夹结构如下：
- frontend
  - resources  #该文件夹用于存放游戏资源。该文件夹为空文件夹。其他脚本使用到时但是默认有文件。可以在这个文件夹下建立一个md文件表示资源存放的位置。
  - scripts  #该文件夹用于存放游戏脚本。
  - scenes  #该文件夹用于存放游戏场景。这里我不放cocos的场景文件。而是用md文件表示场景。
  
 - scenes
  - login.scene.md  #该文件是游戏的登入。
  - farm.scene.md  #该文件是游戏的主场景。

- 001.md  #该文件是以前多个项目的合并最后的一个项目说明文件。仅供参考。
